{
  "name": "legacy-workbench-ci",
  "private": true,
  "version": "0.1.0",
  "description": "Compatibility entry point for existing CI; the UI lives in dashboard/ and frontend/.",
  "scripts": {
    "postinstall": "npm --prefix ../frontend ci",
    "lint": "npm --prefix ../frontend run typecheck",
    "test": "npm --prefix ../frontend test",
    "build": "npm --prefix ../frontend run build"
  }
}
