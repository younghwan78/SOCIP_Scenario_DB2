# Level0 V2 Task4-10 Execution Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development for each implementation slice. Keep this plan updated with checkbox status as work completes.

**Goal:** Finish the remaining Level0 v2 work from the current implementation state: resource-mode API projection, buffer-aware topology projection, Pipeline Viewer integration, Exynos2600 fixture coverage, docs, and verification.

**Architecture:** Keep the existing `ViewResponse` contract and `level0_resource_overview` payload. Add a dedicated `level=0&mode=resource` response for the resource dashboard and a revised `level=0&mode=topology` response that projects active canonical graph nodes plus explicit buffer nodes. Keep `mode=architecture` backward compatible for legacy callers.

**Tech Stack:** Python 3.11+, Pydantic v2, FastAPI, SQLAlchemy-backed canonical graph repository, Streamlit, ELK/SVG renderer, pytest.

---

## Current-State Baseline

- Completed and committed: contract models, resource overview rows, sensor/display summaries, simulation metric attachment, Streamlit resource table, vertical layout, subsystem row colors.
- Current gap: `project_level0()` still routes only legacy `architecture` and legacy/fallback `topology`; `mode=resource` is not implemented.
- Current UI gap: Level0 still loads `architecture` + `topology` and renders two ELK canvases after the resource table.
- Current fixture gap: no broad test reads the Exynos2600 fixture bundle to prove Level0 v2 covers camera/video/game/audio families.
- Current docs gap: Read API docs and README do not describe the Level0 v2 `resource`/`topology` split.

---

## Task 4: Topology Graph Projection

**Files:**
- Modify: `src/scenario_db/view/level0_v2.py`
- Modify: `src/scenario_db/view/service.py`
- Create: `tests/unit/view/test_level0_v2_topology.py`

- [x] Write failing tests for `mode=resource` and `mode=topology`.
- [x] Require topology projection to include active IP/SW nodes and explicit `buf-*` memory nodes for buffered handoffs.
- [x] Require buffered edges to split `producer -> buffer -> consumer`.
- [x] Implement pure helpers in `level0_v2.py` for resource/topology node and edge payloads.
- [x] Route `mode=resource`, `mode=topology`, and legacy `mode=architecture` in `project_level0()`.
- [x] Verify existing runtime projection and simulation overlay tests still pass.

## Task 5: Exynos2600 Fixture Coverage

**Files:**
- Create: `tests/unit/view/test_level0_v2_exynos_fixtures.py`
- Modify if needed: `src/scenario_db/view/level0_v2.py`

- [x] Parse representative YAML files under `db_fixtures_Exynos2600_S26Plus`.
- [x] Build in-memory `CanonicalScenarioGraph` objects without Docker/PostgreSQL.
- [x] Assert camera fixtures expose sensor and display summaries.
- [x] Assert video/YouTube fixtures keep codec/GPU/DPU/panel paths.
- [x] Assert game fixtures include CPU/GPU/NPU/DPU/panel and buffered handoffs where applicable.
- [x] Assert audio fixtures include audio or CPU task resources without camera-only assumptions.

## Task 8: Pipeline Viewer Level0 UI Integration

**Files:**
- Modify: `dashboard/pages/2_Pipeline_Viewer.py`
- Modify: `tests/unit/dashboard/test_level0_resource_overview.py`

- [x] Update source-level UI test to require `mode="resource"` and `mode="topology"` for Level0.
- [x] Load `resource_view` as the Level0 primary view.
- [x] Load `topo_view` as the only Level0 ELK graph.
- [x] Remove Level0 architecture canvas from the normal Level0 page.
- [x] Keep Level1/Level2 load and render flow unchanged.

## Task 9: API Contract And Documentation

**Files:**
- Modify: `docs/read-api-contract.md`
- Modify: `README.md`
- Create: `tests/unit/view/test_level0_v2_docs.py`

- [x] Add failing doc/source test requiring `level=0&mode=resource`, `level0_resource_overview`, and `Scenario Resource Overview`.
- [x] Document Level0 modes: `resource`, `topology`, and legacy `architecture`.
- [x] Document endpoint and response payload expectations.
- [x] Update README viewer verification commands.

## Task 10: Final Verification

- [x] Run focused Level0 tests.
- [x] Run dashboard viewer tests.
- [x] Run runtime projection and simulation overlay tests.
- [x] Run full unit suite.
- [x] Restart FastAPI on `http://127.0.0.1:18000/api/v1` if needed.
- [x] Manually query Level0 resource/topology for camera, game, YouTube/video, and audio scenarios.

---

## TDD Rules

- Start every behavior change with a focused failing test.
- Keep `architecture` mode stable while `resource` and `topology` become the default Level0 UI pair.
- Treat `graph.pipeline_nodes` and `graph.pipeline_edges` as the effective resolved topology.
- Prefer adding pure helper tests before UI source tests.
- Run focused tests after each task before moving to the next task.
